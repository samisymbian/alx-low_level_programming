#include "main.h"

/**
 * main - prints a quote
 * @c: the character to be checked
 * Return: 0 always
 */
int main(int c)
{_puts("\"At the end of the day, my goal was to
		    be the best hacker\"\n\t- Kevin Mitnick");
return (0);
}
